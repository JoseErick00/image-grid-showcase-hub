import CampaignTemplate from '@/components/campaigns/CampaignTemplate';
import { selCozinhaData } from './data/selCozinhaData';

const BrasilCasaSelCozinha = () => {
  return <CampaignTemplate config={selCozinhaData} />;
};

export default BrasilCasaSelCozinha;
