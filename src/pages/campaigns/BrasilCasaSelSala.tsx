import CampaignTemplate from '@/components/campaigns/CampaignTemplate';
import { selSalaData } from './data/selSalaData';

const BrasilCasaSelSala = () => {
  return <CampaignTemplate config={selSalaData} />;
};

export default BrasilCasaSelSala;
